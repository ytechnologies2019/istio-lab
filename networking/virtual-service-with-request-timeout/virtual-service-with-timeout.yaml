apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: http-vs
  namespace: istio-system
spec:
  hosts:
  - https.default.svc.cluster.local
  http:
  - timeout: 5s
    route:
    - destination:
        host: httpd.default.svc.cluster.local
